"use client";

import { useReducedMotion } from "framer-motion";

/**
 * Re-export de Framer Motion's useReducedMotion para uso consistente.
 * Retorna true si el usuario prefiere movimiento reducido.
 */
export { useReducedMotion };
